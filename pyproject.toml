[project]
name = "nexus-pulse"
version = "1.0.0"
description = "Nexus Pulse / SignalWatch — Intelligent Market Delta Attention Engine"
requires-python = ">=3.12"
dependencies = [
  "fastapi==0.141.1",
  "uvicorn==0.52.4",
  "sqlalchemy==2.0.52",
  "pydantic==2.13.5",
  "starlette==1.6.0",
  "websockets==14.2",
  "greenlet==3.5.5",
]

[tool.vercel]
entrypoint = "backend.main:app"
